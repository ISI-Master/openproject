name: Sync from Upstream
on:
  schedule:
    - cron: '0 1 * * *'
  workflow_dispatch:
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: dev
          fetch-depth: 1
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Sync from upstream
        run: |
          git remote add upstream https://github.com/opf/openproject.git
          git fetch upstream dev --depth=1
          git merge upstream/dev --allow-unrelated-histories -m "Sync from upstream" || true
          git push origin dev
